
<?php
if($this->router->fetch_class()!='holy_place' && $page_name != 'request')
{
?>
<div id="latest_news">
<?php //pr($scrolling_news);?>
              <h4>Latest News:</h4>
              <div class="bargaining">
                    <div id="sub-nav">
                          <div class="ticker"><span class="ticker-holder">
                          <?php if(count($scrolling_news) >0 ){ 
									$i = 1;
									$count= count($scrolling_news);
								   foreach($scrolling_news as $key=> $val)
								   {
									  $CSS = (($count-1) != $val['title'])?'|':''; 
									 
							?>
                           <a href="<?=$val['link']?>" target="_blank"><?=$val['title']?></a><?=$CSS?>
                             <?php  $i++;
							} 
						}
					?>
                           </span></div>
                    </div>
              </div>
</div>
 <?php
 }
 ?>      
 
 <?php  $page_excluded = array('prayer-wall-home',
							 'view-all-prayer-request', 'manage-my-prayer-request', 'manage-my-commitments', 		 
							 'prayer-wall-request-detail','intercession-testimony','prayer-wall-testimony','search-prayer-request' 	
							 ,'e-intercession-request-detail','prayer-group-details', 'prayer-group','search-and-invite'
							 ,'search-and-join-prayer-group','library','five-a-day','word-for-today'
							 ,'upcoming-events');
	if(!in_array($page_name ,$page_excluded) || $show_marquee == 1){
?>
<script type="text/javascript">
	
	var ticker_holder = $('.bargaining').get(0);
	var ticker_text = $('.ticker').get(0);
	var ticker_pos = ticker_text.parentNode.offsetWidth;
	
	var ticker_data = $(ticker_holder).html();
	$(ticker_text).parent().html('<marquee scrollamount="3" scrolldelay="20">' + ticker_data + '</marquee>');
	
	$('#sub-nav').hover
	(
	function() { $('marquee', this).get(0).stop();  },
	function() { $('marquee', this).get(0).start(); }
	);

</script>

<?php } ?>          
                  
         